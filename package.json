{
  "name": "tallier",
  "version": "1.0.0",
  "private": true,
  "description": "Tallier tally counter app",
  "packageManager": "bun@1.3.10",
  "scripts": {
    "clean": "rm -rf public/app.js public/sw.js dist",
    "typecheck": "bunx tsc -p tsconfig.client.json && bunx tsc -p tsconfig.server.json",
    "build": "bun run clean && bun run typecheck && bun build ./src/client/app.ts ./src/client/sw.ts --outdir ./public --target browser",
    "start": "bun run build && bun run ./src/server/server.ts",
    "dev": "bun run build && bun --watch ./src/server/server.ts"
  },
  "devDependencies": {
    "bun-types": "^1.3.10",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}
